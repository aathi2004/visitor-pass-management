# Backend base URL used by the built app.
# Leave empty in local dev (the Vite dev server proxies /api to http://localhost:5000).
# Set to your deployed backend URL for production builds, e.g.:
# VITE_API_URL=https://visitor-pass-backend.vercel.app
VITE_API_URL=
